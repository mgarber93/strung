{
  "name": "strung",
  "version": "1.0.0",
  "description": "A string loader",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "init": "mkdir -p test/public",
    "start": "node index.js --verbose",
    "test": "jest --env=node --colors --coverage test",
    "test:watch": "jest --env=node --colors --watch test",
    "lint": "standard --fix"
  },
  "author": "mgarber93",
  "license": "ISC",
  "devDependencies": {
    "jest": "^23.4.2",
    "standard": "^11.0.1"
  },
  "standard": {
    "globals": [ "expect", "test" ]
  },
  "engines": {
    "node": ">=7.0"
  }
}
