{
  "name": "strung",
  "version": "1.0.0",
  "description": "A string loader",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "init": "mkdir -p test/public",
    "start": "node build.js --verbose",
    "test": "jest --env=node --colors --coverage test",
    "test:watch": "jest --env=node --colors --watch test",
    "lint": "standard --fix"
  },
  "author": "Matt Garber",
  "contributors": [
    "Matt Garber <mgarber93@gmail.com>"
  ],
  "license": "ISC",
  "devDependencies": {
    "jest": "^23.5.0",
    "standard": "^11.0.1"
  },
  "standard": {
    "globals": [
      "expect",
      "test"
    ]
  },
  "engines": {
    "node": ">=7.0"
  }
}
